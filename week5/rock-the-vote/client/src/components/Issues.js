import React from 'react';

export default function Issues(props){
    //const { issues } = props

    return (
        <div>
            <h1>Issues Page</h1>
            {/* {issues.map(issue => <ComName {...issue} key = {issue._id} />)} */}
        </div>
    )
}

//All week 6